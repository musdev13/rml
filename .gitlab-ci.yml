workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
    - when: never

stages:
  - test

test-runner:
  stage: test
  tags:
    - linux
  script:
    - echo "=== Runner info ==="
    - whoami
    - pwd
    - echo "$HOME"
    - uname -a
